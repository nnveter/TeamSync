import React from 'react';

const Channel = (props) => {
    return (
        <div className="channelContainer">
            <text>{props.title}</text>
        </div>
    );
};

export default Channel;